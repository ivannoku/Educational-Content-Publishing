steps:
#  - name: 'gcp-storage-sync'
  - name: qcr.io/cloud-builders/gsutil
    args:
      - 'rsync'
      - '-m'      
      - '-d'
      - '-r'
#      - 'gs://${_GCS_BUCKET}'
      - 'gs://educational-content-publish-tbucket'
#substitutions:
#  _GCS_BUCKET: null