steps:
#  - name: 'gcp-storage-sync'
  - name: qcr.io/cloud-builders/gsutil
    args:
      - '_m'
      - 'rsync'
      - '-d'
      - '=r'
      - 'dags'
      - 'qs: //${_GCS_BUCKET}'

substitutions:
  _GCS_BUCKET: null