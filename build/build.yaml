steps:
#  - name: 'gcp-storage-sync'
  - name: qcr.io/cloud-builders/gsutil
    args:
      - '-m'
      - 'rsync'
      - '-d'
      - '-r'
      - 'gs://${_GCS_BUCKET}'

substitutions:
  _GCS_BUCKET: null